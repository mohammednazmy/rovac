# RPLIDAR C1 parameters for rplidar_ros2_driver (frozenreboot)
# DTOF LIDAR: 12m range, ~10Hz, 5KHz sample rate
# USB via CP2102N → /dev/rplidar_c1, baud 460800
rplidar_node:
  ros__parameters:
    serial_port: "/dev/rplidar_c1"
    serial_baudrate: 460800
    frame_id: "laser_frame"
    angle_compensate: true
    scan_mode: "Standard"
    rpm: 0
    interpolated_rays: false
    computed_ray_count: 500
    use_intensities: false
    max_distance: 0.0
    dummy_mode: false
    publish_tf: false
    qos_policy: "sensor_data"
    # Raised from 3 → 10 to tolerate transient CP2102N USB control
    # transfer timeouts on Pi 5 (request 0x12 PURGE status -110).
    # SDK grab_scan_data() blocks ~2s per attempt, so 10 retries = ~20s
    # tolerance before triggering a full hardware reset.
    max_retries: 10
    angle_offset: 0.0
    publish_point_cloud: false
    intensities_as_angles: false
